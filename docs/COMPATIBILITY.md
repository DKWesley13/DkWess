# Compatibility

SecureRepo targets Python 3.11+ and is designed for Windows, Linux and macOS.

The CI suite exercises the package and CLI on GitHub-hosted Ubuntu, macOS and Windows runners. The runtime uses Python's standard library only.

Supported repository checks are intentionally format-specific; unsupported manifests or workflow semantics can remain outside implemented coverage. Use capability coverage and documented limitations when interpreting results.
